{
  "name": "wfp-invoice-generator-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run install-all && npm run build --prefix client",
    "start": "node server/index.js"
  }
}